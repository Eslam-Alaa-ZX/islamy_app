import 'package:flutter/material.dart';

class IntroTap4 extends StatelessWidget {
  const IntroTap4({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("Tap4", style: Theme.of(context).textTheme.titleMedium),
      ),
    );
    ;
  }
}
