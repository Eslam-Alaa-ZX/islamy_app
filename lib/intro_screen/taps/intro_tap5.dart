import 'package:flutter/material.dart';

class IntroTap5 extends StatelessWidget {
  const IntroTap5({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("Tap5", style: Theme.of(context).textTheme.titleMedium),
      ),
    );
    ;
  }
}
